题目：最大化监控边界区域

你负责一个 n x n 网格化区域的安防系统规划，该区域初始时未被任何网络摄像机覆盖（视为“安全区”）。你可以执行以下操作任意次：

安装/调整摄像机： 选择任意一列 j 和该列中的任意一行 i。你可以在该列 j 的顶部安装一个向下拍摄的网络摄像机（或调整其俯仰角），使其监控范围覆盖该列从最顶部（第 0 行）一直到第 i 行（包含）的所有区域。这些被覆盖的区域视为“监控区”。

规划完成后，你需要评估系统的有效性。评估规则关注那些本身未被监控但紧邻监控区的“边界风险点”：

如果一个网格点 (i, j) 是安全区（未被覆盖），并且它紧邻的左侧 (i, j-1) 或右侧 (i, j+1) 网格点中至少有一个是监控区（被覆盖），那么该点 (i, j) 就被识别为一个“边界风险点”。

你的目标是：通过执行任意次数的安装/调整摄像机操作，最终使得整个网格区域中识别出的 “边界风险点”的总数量最大化。

请设计你的摄像机部署方案（即选择在哪些列 j 的哪些行 i 执行操作），并返回最终能够达到的最大“边界风险点”数量。

输入:

一个整数 n (1 <= n <= 150)，表示监控区域的大小是 n x n。

输出:

一个整数，表示通过最优的摄像机部署方案，能够达到的最大“边界风险点”数量。

