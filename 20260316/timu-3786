相机通信消耗之和
在大规模 IPC（网络摄像机）集群化部署与智能运维系统中，整个监控网络被抽象为一棵无向树形拓扑结构（无环连通图），用于表征 n 台 IPC 设备（节点编号从 0 到 n-1）之间的物理级联链路与数据传输路径。每台 IPC 设备均被赋予唯一的节点编号，
设备间的直连级联关系由长度为 n-1 的二维数组edges描述，其中edges[i] = [ui, vi]表示编号为 ui 的 IPC 与编号为 vi 的 IPC 之间存在一条无向的物理级联链路，该链路是两台设备间数据交互的唯一直连通道。
为实现精细化的运维管理与流量调度，系统会为每台 IPC 分配一个业务组标签，由长度为 n 的整数数组group定义（group[i]表示编号为 i 的 IPC 所属的业务组）。若两台 IPC 的业务组标签相同，则认为它们归属于同一业务组，需进行跨设备的业务数据交互。
在 IPC 集群的流量成本核算体系中，通信消耗被严格定义为：两台同组 IPC 之间的唯一数据传输路径上所经过的物理级联链路数量（即路径上的边数）。例如，若 IPC A 与 IPC B 的传输路径为 A→C→D→B，则该路径包含 3 条链路，通信消耗为 3。
系统的核心算力需求为：计算所有满足以下条件的无序 IPC 节点对 (u, v) 的通信消耗总和：
u ≠ v（排除同一台 IPC 的自交互）；
group [u] == group [v]（仅统计同业务组的 IPC 对）。
若不存在符合条件的 IPC 节点对，返回 0。

补充约束与说明
树形拓扑的节点数 n 满足：1 ≤ n ≤ 100000（覆盖中小型 IPC 集群规模）；
业务组标签为整数型，取值范围[1,20]（可覆盖不同业务域的分组标识）；
无向树的边无权重，且任意两台 IPC 之间的传输路径唯一（树形结构的核心特性）；
无序节点对的定义：(u, v) 与 (v, u) 视为同一对，仅需计算一次通信消耗，不得重复累加。
输入保证 edges 表示一棵有效的树。

示例 1
输入：
第一行n n表示相机数量
接下来n-1行，每行两个整数u v，表示IPC u和v之间存在一条边；
最后一行n个整数，表示每个IPC的组标签。
5 4
0 1
0 2
1 3
1 4
1 1 2 1 2
符合条件的节点对：
组 1 的节点对：(0,1)（路径边数 1）、(0,3)（路径边数 2）、(1,3)（路径边数 1）；
组 2 的节点对：(2,4)（路径边数 3）；
通信消耗总和：1+2+1+3 = 7，因此返回 7。
输出
所有相机通信消耗之和
7