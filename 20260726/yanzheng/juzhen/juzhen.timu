1360: 矩阵填数

时间限制：2 Sec    内存限制：512 MB

题目描述

给定一个 n x n 的矩阵，填入 [1, n^2] 范围内的整数。定义 ai 为第 i 行的最小值，令集合

S = {a1, a2, ..., an} ∩ {1, 2, ..., n}

求所有可能的 S 的大小之和，即 Σ |S|（mod 1000000007）。

其中，|S| 代表集合中元素的个数。例如 {1, 2, 3} 的 |S| = 3。

输入格式

第一行包含一个整数 T（1 <= T <= 20），表示有 T 组测试数据。
接下来 T 行，每行一个整数 n（1 <= n <= 5000）。

输出格式

输出 T 行，每行一个整数，对应每个 Case 的答案。

样例输入

2
1
2

样例输出

1
40

提示

n = 1 时，矩阵只有一种 [1]，此时 S = {1}，|S| = 1。
n = 2 时，不管什么样的矩阵，都只有 |S| = 1 和 |S| = 2 两种情况，具体可见题面示例。
