安全校验

  小华是公司安全部门的研究员，负责评估设备系统中加密模块的可靠性。已知某批设备使用基于双核心参数生成的加密凭证，
  当任一参数长度不足时将存在高危漏洞。

‌输入说明：‌
凭证密文S 安全阈值L
密文为由数字组成的字符串（4 ≤ 长度 ≤ 100位），代表加密模块生成的双参数（参数为素数）乘积
安全阈值L（2 ≤ L ≤ 1e6）表示参数的最低合规要求


‌输出要求：‌
若存在任意核心参数长度min_param < L，输出"invalid min_param"，其中min_param为实际使用的最小参数
若双参数均 ≥ L，输出"valid"

‌示例：‌

输入：
143 20

输出：
invalid 11
（解释：143=11×13，因11<20存在安全风险）

‌技术提示：‌
需高效处理大数分解问题，重点检测是否存在小于L的因子。这与评估RSA密钥是否使用弱质数的现实渗透测试场景高度一致。

原始
描述

年轻且非常有前途的密码学家 Odd Even 已经实现了一个拥有数千名用户的大型系统的安全模块，该模块现在正在他的公司中使用。加密密钥是由两个质数的乘积创建的，并且被认为是安全的，因为没有已知的方法可以有效地分解此类产品。
Odd Even 没有想到的是，一个 key 中的两个 factor 都应该很大，而不仅仅是他们的 product。现在，系统的某些用户可能具有弱密钥。为了不被解雇，Odd Even 秘密检查了所有用户的密钥，以检查它们是否足够强大。他使用非常强大的 Atari，并且在检查老板的钥匙时特别小心。
输入

输入由不超过 20 个测试用例组成。每个测试用例都是一行，其中包含整数 4 <= K <= 10100 和 2 <= L <= 106。K 是键本身，是两个素数的乘积。L 是键中因子所需的最小大小。input set 由 K = 0 和 L = 0 的情况终止。
输出

对于每个数字 K，如果其其中一个因子严格小于所需的 L，则程序应输出 “BAD p”，其中 p 是 K 中的最小因子。否则，应输出 “GOOD”。大小写应用换行符分隔。
样本输入

143 10
143 20
667 20
667 30
2573 30
2573 40
0 0
示例输出

GOOD
BAD 11
GOOD
BAD 23
GOOD
BAD 31